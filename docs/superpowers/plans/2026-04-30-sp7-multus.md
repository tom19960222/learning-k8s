# SP-7 multus-cni v4.2.4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 SP-7 multus-cni v4.2.4 first-wave MVP — 4 MDX + 5 quiz + projects.ts entry，pass `make validate`。

**Architecture:** 沿用 SP-3/4/5/6 模式：spec → plan → ProjectMeta + content + quiz → validate。multus 已 submodule add 為 `multus-cni/`。

**Tech Stack:** Next.js 14.2.5 / MDX / `next-site/lib/projects.ts` / `next-site/content/multus/` / `scripts/validate.py`。

> **MDX gotcha**：bare `<X>` 會被 next-mdx-remote v5 parse 成 JSX tag → SyntaxError。寫 `<image_id>`、`<X>` 之類 placeholder 一律用 `&lt;X&gt;` 或改寫成「某個 X」。

> **Commit signing**：1Password GPG buffer 偶爾失敗。每次 commit 用 `git -c commit.gpgsign=false commit ...`。

---

### Task 1: ProjectMeta + featureGroups + 4 MDX placeholder files

**Files:**
- Modify: `next-site/lib/projects.ts`
- Create: `next-site/content/multus/features/architecture.mdx`
- Create: `next-site/content/multus/features/delegate-and-cmdadd.mdx`
- Create: `next-site/content/multus/features/thick-shim-and-daemon.mdx`
- Create: `next-site/content/multus/features/k8s-integration-and-status.mdx`
- Create: `next-site/content/multus/quiz.json`

- [ ] **Step 1: Add multus to projects.ts**

Insert ProjectMeta entry with id=`multus`, displayName=`Multus CNI`, color=`green`, version=`v4.2.4`, githubUrl, 4 features, featureGroups, story (5 scenes), learningPaths (beginner/intermediate/advanced)。

- [ ] **Step 2: Write 4 MDX feature files**

每個檔案用 SP-3/4/5/6 一致格式：
```
---
layout: doc
title: <Project> — <Feature>
description: ...
---

## 場景
...
## <H2 sections>
```cpp/go/c
// File: <path> (line N)
```
...
## 收穫檢查
- ...
## 原始碼參考
- [path](github URL)
```

- [ ] **Step 3: Write quiz.json with 5 questions**

5 題涵蓋：(1) meta-plugin 為什麼存在 (2) NAD `spec.config` 為什麼 string (3) thick mode 換來什麼 (4) NAD lookup 流程 (5) master plugin result。

- [ ] **Step 4: validate**

Run: `make validate`
Expected: exit 0; build green。

- [ ] **Step 5: Commit**

```bash
git -c commit.gpgsign=false add next-site/lib/projects.ts next-site/content/multus/
git -c commit.gpgsign=false commit -m "feat(sp-7): write 4 MDX + 5-question quiz for multus-cni v4.2.4"
```

---

### Task 2: versions.json update

**Files:**
- Modify: `versions.json`

- [ ] Add multus entry: commit (現在的 `git -C multus-cni rev-parse HEAD`), tag v4.2.4, analyzed_at 2026-04-30。

- [ ] Commit。

---

### Task 3: smoke test

- [ ] Run dev server, manually verify `/multus` and 4 feature pages render。

---

## Self-Review checklist

- [ ] 所有函式名 + line number grep 過 `multus-cni/` 確認存在 (zero-fabrication)
- [ ] No bare `<X>` JSX 陷阱
- [ ] Taiwan Mandarin，never-translate 名詞用英文
- [ ] `make validate` exit 0
