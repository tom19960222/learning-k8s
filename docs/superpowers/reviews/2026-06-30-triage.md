# Triage — ceph-incident-bundle hardening (2026-06-30)

Consolidated from Code Reviewer + SRE + codex. Scope = correctness/robustness/redaction hardening; no new features. `R#` = reviewers that raised it (C=CodeReviewer, S=SRE, X=codex).

## In-scope — Security / redaction (safety boundary correctness)
| id | sev | file | issue | fix | reviewers |
|----|-----|------|-------|-----|-----------|
| S1 | HIGH | common.sh | multi-line PEM body leaks (only header redacted) | stateful: BEGIN..KEY → redact until END | C |
| S2 | HIGH | common.sh/collect.sh | `*.gz` logs never redacted | gunzip→redact→gzip in redact pass | C,X |
| S3 | HIGH | common.sh | ceph `key = AQB..==` / base64 blobs not matched | add key/caps/base64 patterns | C |
| S4 | HIGH | verify-bundle.sh | blocks filenames only; `*.pem/*.key/*.crt`+content pass | add patterns + content grep `BEGIN * PRIVATE KEY` / `key =` | C |
| S5 | MED | verify-bundle.sh | newline filename smuggles past `case` | `find -print0` + `read -r -d ''` | C |
| S6 | MED | common.sh | `chmod --reference` GNU-only (macOS workstation) | portable `stat` mode read | C,S,X |

## In-scope — Correctness / exit-code
| id | sev | file | issue | fix | reviewers |
|----|-----|------|-------|-----|-----------|
| C1 | HIGH | collect.sh | per-command `--timeout` also wraps whole node SSH → slow node killed | add `--node-timeout` (default 600) for outer wrapper | C,S,X |
| C2 | HIGH | collect.sh | no trap → workdir + `.node-*.tar.gz` leak on Ctrl-C/fail | `trap cleanup EXIT INT TERM` (honor `--keep-workdir`) | S,X |
| C3 | HIGH | collect.sh | verify under `set -e` aborts before tar → evidence destroyed | capture verify rc, still tar, exit 1 only on real fail | S,C |
| C4 | HIGH | collect.sh | truncated/timeout node tar still counted `node_ok` | require node `manifest.jsonl` present post-extract else rc=2 | S |
| C6 | MED | collect.sh | malformed HOSTS: `\|\| die` binds to pipeline → silent empty | parse once; bad entry → append_error+continue+rc=2 | C,S |
| C7 | MED | common.sh | rc==124 timeout indistinguishable; truncation invisible | mark `timed_out`, append `# TRUNCATED` trailer, summary count | S |
| C8 | HIGH | collect-node.sh | dmesg/journalctl truncated by small per-cmd timeout | heavier dedicated timeout + C7 marker | S |
| R6 | HIGH | common.sh/collect.sh | missing `timeout` silently unbounds (macOS/minimal) | prefer `gtimeout`; warn loudly if neither | X |

## In-scope — Robustness
| id | sev | file | issue | fix | reviewers |
|----|-----|------|-------|-----|-----------|
| R1 | MED | collect-cluster-cephadm.sh | cluster SSH no ConnectTimeout/ServerAlive; `sudo` no `-n`; hardcoded sudo when root | add ssh opts; `sudo -n`; root branch | C,S,X |
| R2 | MED | collect-node.sh | oversized log silent drop | `tail -c cap` + `.TRUNCATED` marker | C,S,X |
| R3 | MED | collect-cluster-cephadm.sh | crash-id grep `id\|name` too broad | anchor `crash_id` | C |
| R4 | MED | collect-cluster-rook.sh | missing kubectl/ns returns 0 → false success in explicit rook mode | explicit mode → rc=2; auto keeps skip | X |
| R5 | MED | collect.sh | remote `tar -xzf -`/`-czf -` BusyBox portability | `gzip -dc \| tar -xf -` form | C |

## In-scope — Low / shellcheck
| id | sev | file | issue | fix |
|----|-----|------|-------|-----|
| L1 | LOW | collect-node.sh, collect-cluster-cephadm.sh | `cmd=($string)` SC2206 word-split | specs → real arrays |
| L2 | LOW | collect.sh | macOS bsdtar AppleDouble in final tar | `COPYFILE_DISABLE=1` on final/verify tar |
| L3 | LOW | collect-node.sh | unreadable file → fail not skip in node_file_size | skip sentinel |
| L4 | — | all | shellcheck 13 warn + 4 note → 0 | real fixes over disable |

## Out-of-scope (documented, no behavior change)
- Complete/structured DLP secret scanning (feature) — README already disclaims; add code comment only.
- Parallel node collection (feature).
- Free-space preflight `df` warn (adjacent; skip).
- `node_run_optional` optional-failure = exit-2 (by design per README "已知 optional 非零"); document only.
- Per-invocation `cephadm shell` container load on seed (inherent).

## Execution note
Fixes implemented directly with TDD against the existing harness, grouped by file into logical commits (correctness sensitivity > per-finding subagent round-trips). SRE agent runs Phase 4 lab multi-fault validation; codex does a final independent re-review post-fix.
